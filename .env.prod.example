# Production Environment Configuration for Digital Ocean VPS
# Copy this file to .env.prod and update with your actual values

# Database Configuration
POSTGRES_DB=swgoh_tb_prod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-super-secure-database-password-here

# JWT Configuration (REQUIRED - Generate strong secrets)
JWT_SECRET=your-super-long-and-random-jwt-secret-key-at-least-64-characters-long
JWT_REFRESH_SECRET=your-super-long-and-random-refresh-secret-key-at-least-64-characters-long
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting (15 minutes = 900000ms, 100 requests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend Configuration
VITE_API_URL=https://yourdomain.com/api
VITE_APP_NAME=SWGOH RotE TB Recommended Squads
FRONTEND_PORT=80

# Digital Ocean specific (if using managed database)
# DATABASE_URL=postgresql://username:password@db-hostname:25060/database?sslmode=require

# SSL Configuration (if using Let's Encrypt)
# ENABLE_SSL=true
# DOMAIN=yourdomain.com
# EMAIL=your-email@domain.com

# Backup Configuration
# BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM
# BACKUP_RETENTION_DAYS=7